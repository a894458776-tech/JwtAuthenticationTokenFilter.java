<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kylin.kton.system.mapper.KtonIpStaticMapper">
    
    <resultMap type="KtonIpStatic" id="KtonIpStaticResult">
        <result property="id"    column="id"    />
        <result property="title"    column="title"    />
        <result property="type"    column="type"    />
        <result property="icon"    column="icon"    />
        <result property="status"    column="status"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectKtonIpStaticVo">
        select id, title, type, icon, status, create_time, update_time from kton_ip_static
    </sql>

    <select id="selectKtonIpStaticList" parameterType="KtonIpStatic" resultMap="KtonIpStaticResult">
        <include refid="selectKtonIpStaticVo"/>
        <where>  
            <if test="title != null  and title != ''"> and title = #{title}</if>
            <if test="type != null "> and type = #{type}</if>
            <if test="icon != null  and icon != ''"> and icon = #{icon}</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
        </where>
    </select>
    
    <select id="selectKtonIpStaticById" parameterType="Long" resultMap="KtonIpStaticResult">
        <include refid="selectKtonIpStaticVo"/>
        where id = #{id}
    </select>

    <insert id="insertKtonIpStatic" parameterType="KtonIpStatic" useGeneratedKeys="true" keyProperty="id">
        insert into kton_ip_static
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="title != null and title != ''">title,</if>
            <if test="type != null">type,</if>
            <if test="icon != null and icon != ''">icon,</if>
            <if test="status != null and status != ''">status,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="title != null and title != ''">#{title},</if>
            <if test="type != null">#{type},</if>
            <if test="icon != null and icon != ''">#{icon},</if>
            <if test="status != null and status != ''">#{status},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateKtonIpStatic" parameterType="KtonIpStatic">
        update kton_ip_static
        <trim prefix="SET" suffixOverrides=",">
            <if test="title != null and title != ''">title = #{title},</if>
            <if test="type != null">type = #{type},</if>
            <if test="icon != null and icon != ''">icon = #{icon},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteKtonIpStaticById" parameterType="Long">
        delete from kton_ip_static where id = #{id}
    </delete>

    <delete id="deleteKtonIpStaticByIds" parameterType="String">
        delete from kton_ip_static where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>